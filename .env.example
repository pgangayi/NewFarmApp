# Environment variables (example) for Farmers Boot — Phase 0
# Copy this file to `.env` or set secrets in your deployment platform.

# --- Cloudflare D1 Database
# For production (Cloudflare)
DATABASE_URL=postgresql://postgres:password@your-db.supabase.co:5432/postgres

# For local development (Cloudflare D1 with Wrangler)
# DATABASE_URL=postgres://postgres:postgres@localhost:54322/postgres

# --- Map / 3rd party
VITE_MAPBOX_TOKEN=your-mapbox-token

# --- Cloudflare Pages / Functions (if used)
CF_ACCOUNT_ID=your-cloudflare-account-id
CF_API_TOKEN=your-cloudflare-api-token

# --- Monitoring & Security (Phase 4)
SENTRY_DSN=your-sentry-dsn
RATE_LIMIT_KV_ID=your-kv-namespace-id
ENVIRONMENT=development

# --- Optional / Local dev
PORT=3000
NODE_ENV=development

# Notes:
# - Do NOT commit service role keys to source control. Keep them in your CI / cloud secrets.
# - For local development, use Cloudflare D1 with Wrangler CLI: https://developers.cloudflare.com/d1/
# - Replace placeholders with real values before running migrations or CI that needs DB access.