{
  "name": "farmers-boot-retry",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start:e2e": "node ./scripts/start-e2e-servers.js",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "deploy": "./scripts/deploy/deploy.sh",
    "deploy:windows": "powershell -ExecutionPolicy Bypass -File ./scripts/deploy/deploy.ps1",
    "build": "cd frontend && npm run build",
    "build:ci": "cd frontend && VITE_MAPBOX_TOKEN=dummy_token npm run build",
    "dev": "cd frontend && npm run dev",
    "dev:local": "wrangler pages dev frontend --local --port 8788",
    "dev:functions": "wrangler dev --local --port 8787",
    "setup:local": "echo 'Setting up local development environment...' && echo '1. Install Wrangler CLI: npm install -g wrangler' && echo '2. Start Cloudflare D1 locally: wrangler d1 start' && echo '3. Copy .env.example to .env and fill in local values' && echo '4. Run migrations: wrangler d1 execute db --local --file=./migrations/0001_d1_complete_schema.sql' && echo '5. Run: npm run dev:local'",
    "setup:local:windows": "powershell -ExecutionPolicy Bypass -File ./setup-local.ps1",
    "setup:local:unix": "bash ./setup-local.sh"
  }
}
