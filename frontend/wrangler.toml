name = "farmers-boot"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# Pages configuration
pages_build_output_dir = "dist"

# Environment variables for production
[vars]
VITE_API_BASE_URL = "https://farmers-boot-retry.pgangayi.workers.dev"