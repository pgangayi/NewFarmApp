name = "farmers-boot-retry"
main = "index.js"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

[vars]
ENVIRONMENT = "development"
JWT_SECRET = "farmers_boot_jwt_secret_2024_secure_random_key_generated_for_authentication_tokens"
RESEND_API_KEY = "re_gYQWMQY1_5fqkWdXxrJXVSuXAMygXzGLs"
FROM_EMAIL = "noreply@farmersboot.com"
APP_URL = "http://localhost:3000"
NODE_ENV = "development"

# Database binding for D1
[[d1_databases]]
binding = "DB"
database_name = "farmers_boot_db"
database_id = "local"

# Environment variables should be set externally:
# JWT_SECRET - for JWT token signing
# APP_URL - for generating reset links
# SENTRY_DSN - for error tracking (optional)

# For local development
[dev]
port = 8787